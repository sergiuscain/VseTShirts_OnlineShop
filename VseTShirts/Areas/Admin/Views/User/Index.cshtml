@model List<UserAccount>

<div class="row">
     @await Html.PartialAsync("AdminLeftMenu")
    @if (Model is null || Model.Count < 1)
    {
        <div class="col-3 btn border-danger">
             <h4 class="align-content-center">Упс, Список пользователей пуст</h4>
        </div>
    }
    else
    {
        <div class="col-9">
            <h3>Список пользователей:</h3>
           <table class="table table-striped">
            <tr>
                <th>Логин</th>
                <th>Email</th>
                <th>ID</th>
                <th>Действия</th>
            </tr>
            @foreach (var user in Model)
            {
                <tr>
                    <td>@user.UserName</td>
                    <td>@user.Email</td>
                    <td>@user.Id</td>
                    <td>
                       <div class="dropdown">
                        <button class="btn btn-dark dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
                            Действия:
                        </button>
                        <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                                    <li><a class="dropdown-item" asp-area="Admin" asp-controller="User" asp-action="Block">Заблокировать</a></li>
                                     <li><a class="dropdown-item" asp-area="Admin" asp-controller="User" asp-action="Del" >Удалить аккаунт</a></li>
                                     <li><a class="dropdown-item" asp-area="Admin" asp-controller="User" asp-action="EditRole" >Права доступа</a></li>
                        </ul>
                       </div>
                    </td>
                </tr>
            }
         </table>
       </div>
    } 
</div>